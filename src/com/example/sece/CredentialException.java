package com.example.sece;

public class CredentialException {

	/** Here, the exception will be thrown if there are no credentials. Such as if no credentials, then throw exception.
	 * 
	 * 
	 * If the database does not have credentials, a message will be sent to client saying that there are no previous credentials.
	 * 
	 * 
	 */
	
}
